---
layout: base.njk
title: "Filo — nisslar"
---

<!-- HEADER -->
<header class="fixed top-0 inset-x-0 z-50 backdrop-blur-md bg-white/90 border-b border-gray-200 shadow-sm">
  <div class="max-w-[1440px] mx-auto px-6 md:px-10 h-[72px] md:h-[80px] flex items-center justify-between">
    <a href="/" class="flex items-center gap-3 group" aria-label="nisslar">
      <img src="/assets/favicon.png" alt="" class="h-9 w-9 md:h-10 md:w-10 rounded-md" />
      <span class="font-serif text-[25px] md:text-[30px] leading-none tracking-tightest text-cream whitespace-nowrap">
        nisslar
      </span>
    </a>

    <nav class="hidden md:flex items-center gap-9 text-[15px] tracking-[0.04em] text-cream/70">
      <a href="/" class="hover:text-accent transition lnk" data-i18n="nav.home">Ana Sayfa</a>
      <a href="/fleet/" class="text-accent transition lnk" data-i18n="nav.fleet">Filo</a>
      <a href="/#about" class="hover:text-accent transition lnk" data-i18n="nav.about">Hakkımızda</a>
      <a href="/#faq" class="hover:text-accent transition lnk" data-i18n="nav.faq">SSS</a>
    </nav>

    <div class="flex items-center gap-3 md:gap-5">
      <div class="lang-track flex items-center p-1 rounded-full border border-line bg-ink2/60" data-lang="tr">
        <span class="lang-slider"></span>
        <button type="button" class="lang-pill flex items-center gap-1 md:gap-1.5 px-2.5 md:px-3.5 py-1.5 rounded-full text-[10px] md:text-[11px] font-medium tracking-[0.14em]" data-lang="tr">TR</button>
        <button type="button" class="lang-pill flex items-center gap-1 md:gap-1.5 px-2.5 md:px-3.5 py-1.5 rounded-full text-[10px] md:text-[11px] font-medium tracking-[0.14em]" data-lang="en">EN</button>
      </div>

      <a href="https://turo.com/us/en/host/51476329" target="_blank" rel="noopener" class="hidden md:inline-flex group items-center gap-2.5 bg-gold text-white hover:bg-goldhi transition px-5 py-2.5 rounded-full text-[11.5px] font-semibold tracking-[0.18em] uppercase">
        <span data-i18n="nav.book">Rezerve Et</span>
        <span class="arr -mr-0.5">→</span>
      </a>

      <button class="hamburger md:hidden flex flex-col gap-[5px] w-7 h-7 justify-center items-center" aria-label="Menü">
        <span class="block w-5 h-[1.5px] bg-cream"></span>
        <span class="block w-5 h-[1.5px] bg-cream"></span>
        <span class="block w-5 h-[1.5px] bg-cream"></span>
      </button>
    </div>
  </div>
</header>

<!-- Mobile Menu -->
<div class="mobile-menu fixed inset-0 z-40 bg-white/98 backdrop-blur-lg flex flex-col items-center justify-center gap-8 md:hidden">
  <a href="/" class="mobile-link text-cream text-[18px] font-serif tracking-tightest" data-i18n="nav.home">Ana Sayfa</a>
  <a href="/fleet/" class="mobile-link text-accent text-[18px] font-serif tracking-tightest" data-i18n="nav.fleet">Filo</a>
  <a href="/#about" class="mobile-link text-cream text-[18px] font-serif tracking-tightest" data-i18n="nav.about">Hakkımızda</a>
  <a href="/#faq" class="mobile-link text-cream text-[18px] font-serif tracking-tightest" data-i18n="nav.faq">SSS</a>
  <a href="https://turo.com/us/en/host/51476329" target="_blank" rel="noopener" class="mt-4 group inline-flex items-center gap-2.5 bg-gold text-white hover:bg-goldhi transition px-7 py-3 rounded-full text-[12px] font-semibold tracking-[0.18em] uppercase">
    <span data-i18n="nav.book">Rezerve Et</span>
    <span class="arr">→</span>
  </a>
</div>

<!-- PAGE HERO -->
<section class="pt-[72px] md:pt-[80px]">
  <div class="max-w-[1440px] mx-auto px-6 md:px-10 py-14 md:py-20">
    <div class="mono-lbl text-accent uppercase mb-4">— <span data-i18n="fleet.eyebrow">Filo</span></div>
    <h1 class="font-serif text-[clamp(40px,5.4vw,80px)] leading-[0.96] tracking-tightest text-cream balance">
      <span data-i18n="fleet.title1">Size özel</span> <span class="text-accent italic font-light" data-i18n="fleet.title2">araçlar</span>.
    </h1>
    <p class="mt-4 text-[14px] text-cream/55 max-w-[480px] pretty" data-i18n="fleet.desc">
      Her araç bizzat seçildi. Detaylı temizlik, tam yakıt, anahtar elden teslim — başka türlüsü olmaz.
    </p>
  </div>
</section>

<!-- FILTERS & SORT -->
<section class="border-y border-line bg-ink2">
  <div class="max-w-[1440px] mx-auto px-6 md:px-10 py-5 flex flex-wrap items-center gap-3 md:gap-4">
    <div class="flex items-center gap-2 mr-2">
      <svg viewBox="0 0 20 20" class="w-4 h-4 text-cream/40" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 5h14M5 10h10M7 15h6"/></svg>
      <span class="mono-lbl text-cream/40 uppercase" data-i18n="fleet.filter">Filtre</span>
    </div>

    <!-- Brand filter -->
    <select id="filter-brand" class="fleet-select bg-white border border-line rounded-lg px-3 py-2 text-[13px] text-cream focus:border-gold focus:ring-1 focus:ring-gold outline-none">
      <option value="all" data-i18n-opt="fleet.allBrands">Tüm Markalar</option>
    </select>

    <!-- Category filter -->
    <select id="filter-cat" class="fleet-select bg-white border border-line rounded-lg px-3 py-2 text-[13px] text-cream focus:border-gold focus:ring-1 focus:ring-gold outline-none">
      <option value="all" data-i18n-opt="fleet.allCats">Tüm Kategoriler</option>
    </select>

    <!-- Seats filter -->
    <select id="filter-seats" class="fleet-select bg-white border border-line rounded-lg px-3 py-2 text-[13px] text-cream focus:border-gold focus:ring-1 focus:ring-gold outline-none">
      <option value="all" data-i18n-opt="fleet.allSeats">Tüm Koltuklar</option>
    </select>

    <div class="flex-1"></div>

    <!-- Sort -->
    <div class="flex items-center gap-2">
      <span class="mono-lbl text-cream/40 uppercase hidden sm:inline" data-i18n="fleet.sort">Sırala</span>
      <select id="sort-by" class="fleet-select bg-white border border-line rounded-lg px-3 py-2 text-[13px] text-cream focus:border-gold focus:ring-1 focus:ring-gold outline-none">
        <option value="default" data-i18n-opt="fleet.sortDefault">Varsayılan</option>
        <option value="name" data-i18n-opt="fleet.sortName">İsim (A-Z)</option>
        <option value="year-desc" data-i18n-opt="fleet.sortYear">Yıl (Yeni → Eski)</option>
        <option value="hp-desc" data-i18n-opt="fleet.sortHp">Güç (Yüksek → Düşük)</option>
        <option value="seats-asc" data-i18n-opt="fleet.sortSeats">Koltuk (Az → Çok)</option>
      </select>
    </div>
  </div>
</section>

<!-- FLEET GRID -->
<section class="py-12 md:py-20">
  <div class="max-w-[1440px] mx-auto px-6 md:px-10">
    <div id="fleet-count" class="mono-lbl text-cream/40 mb-6"></div>
    <div id="fleet-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-7 gap-y-10 md:gap-y-14"></div>
    <div id="fleet-empty" class="hidden text-center py-20">
      <p class="text-cream/40 text-[15px]" data-i18n="fleet.empty">Bu kriterlere uygun araç bulunamadı.</p>
    </div>
    <!-- Pagination -->
    <div id="pagination" class="mt-12 flex items-center justify-center gap-2"></div>
  </div>
</section>

<!-- FOOTER (minimal) -->
<footer class="border-t border-line py-10" style="background:#F0F2F5">
  <div class="max-w-[1440px] mx-auto px-6 md:px-10 flex flex-col md:flex-row items-center justify-between gap-4">
    <a href="/" class="flex items-center gap-2">
      <img src="/assets/favicon.png" alt="" class="h-8 w-8 rounded-md" />
      <span class="font-serif text-[20px] leading-none tracking-tightest text-cream">nisslar</span>
    </a>
    <div class="text-[12px] text-cream/40">© 2026 nisslar LLC · <span data-i18n="footer.copy">Tüm hakları saklıdır.</span></div>
  </div>
</footer>

<!-- WhatsApp -->
<a id="whatsapp-btn" href="https://wa.me/18635581238" target="_blank" rel="noopener" aria-label="WhatsApp" class="fixed bottom-6 right-6 z-50 w-14 h-14 bg-[#25D366] hover:bg-[#1EBE57] transition rounded-full flex items-center justify-center shadow-lg shadow-black/20">
  <svg viewBox="0 0 24 24" class="w-7 h-7 text-white" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
</a>

<script>
let FLEET = {{ cars | dump | safe }};
const PER_PAGE = 12;
let currentPage = 1;
let LANG = 'tr';

const I18N = {
  tr: {
    'nav.home':'Ana Sayfa','nav.fleet':'Filo','nav.about':'Hakkımızda','nav.faq':'SSS','nav.book':'Rezerve Et',
    'fleet.eyebrow':'Filo','fleet.title1':'Size özel','fleet.title2':'araçlar',
    'fleet.desc':'Her araç bizzat seçildi. Detaylı temizlik, tam yakıt, anahtar elden teslim — başka türlüsü olmaz.',
    'fleet.filter':'Filtre','fleet.sort':'Sırala',
    'fleet.allBrands':'Tüm Markalar','fleet.allCats':'Tüm Kategoriler','fleet.allSeats':'Tüm Koltuklar',
    'fleet.sortDefault':'Varsayılan','fleet.sortName':'İsim (A-Z)',
    'fleet.sortYear':'Yıl (Yeni → Eski)','fleet.sortHp':'Güç (Yüksek → Düşük)',
    'fleet.sortSeats':'Koltuk (Az → Çok)',
    'fleet.empty':'Bu kriterlere uygun araç bulunamadı.',
    'fleet.showing':'{count} araç gösteriliyor',
    'card.book':'Rezerve Et','card.seats':'koltuk','card.hp':'BG',
    'footer.copy':'Tüm hakları saklıdır.',
  },
  en: {
    'nav.home':'Home','nav.fleet':'Fleet','nav.about':'About','nav.faq':'FAQ','nav.book':'Reserve',
    'fleet.eyebrow':'Fleet','fleet.title1':'Cars curated','fleet.title2':'for you',
    'fleet.desc':'Each car personally selected. Detailed clean, full fuel, handover in person — no other way.',
    'fleet.filter':'Filter','fleet.sort':'Sort',
    'fleet.allBrands':'All Brands','fleet.allCats':'All Categories','fleet.allSeats':'All Seats',
    'fleet.sortDefault':'Default','fleet.sortName':'Name (A-Z)',
    'fleet.sortYear':'Year (New → Old)','fleet.sortHp':'Power (High → Low)',
    'fleet.sortSeats':'Seats (Low → High)',
    'fleet.empty':'No cars match these criteria.',
    'fleet.showing':'{count} cars shown',
    'card.book':'Reserve','card.seats':'seats','card.hp':'HP',
    'footer.copy':'All rights reserved.',
  }
};

function getFilteredSorted() {
  let cars = [...FLEET];
  const brand = document.getElementById('filter-brand').value;
  const cat = document.getElementById('filter-cat').value;
  const seats = document.getElementById('filter-seats').value;
  const sort = document.getElementById('sort-by').value;

  if (brand !== 'all') cars = cars.filter(c => c.brand === brand);
  if (cat !== 'all') cars = cars.filter(c => c.cat[LANG] === cat);
  if (seats !== 'all') cars = cars.filter(c => c.seats === parseInt(seats));

  switch (sort) {
    case 'name': cars.sort((a, b) => a.name.localeCompare(b.name)); break;
    case 'year-desc': cars.sort((a, b) => b.year - a.year); break;
    case 'hp-desc': cars.sort((a, b) => b.hp - a.hp); break;
    case 'seats-asc': cars.sort((a, b) => a.seats - b.seats); break;
  }
  return cars;
}

function populateFilters() {
  const t = I18N[LANG];
  const brandSelect = document.getElementById('filter-brand');
  const catSelect = document.getElementById('filter-cat');
  const seatsSelect = document.getElementById('filter-seats');
  const sortSelect = document.getElementById('sort-by');

  const brands = [...new Set(FLEET.map(c => c.brand))].sort();
  const cats = [...new Set(FLEET.map(c => c.cat[LANG]))].sort();
  const seatVals = [...new Set(FLEET.map(c => c.seats))].sort((a, b) => a - b);

  const prevBrand = brandSelect.value;
  const prevCat = catSelect.value;
  const prevSeats = seatsSelect.value;

  brandSelect.innerHTML = `<option value="all">${t['fleet.allBrands']}</option>` +
    brands.map(b => `<option value="${b}" ${b === prevBrand ? 'selected' : ''}>${b}</option>`).join('');

  catSelect.innerHTML = `<option value="all">${t['fleet.allCats']}</option>` +
    cats.map(c => `<option value="${c}" ${c === prevCat ? 'selected' : ''}>${c}</option>`).join('');

  seatsSelect.innerHTML = `<option value="all">${t['fleet.allSeats']}</option>` +
    seatVals.map(s => `<option value="${s}" ${String(s) === prevSeats ? 'selected' : ''}>${s} ${t['card.seats']}</option>`).join('');

  sortSelect.querySelectorAll('option').forEach(opt => {
    const key = opt.getAttribute('data-i18n-opt');
    if (key && t[key]) opt.textContent = t[key];
  });
}

function renderFleet() {
  const t = I18N[LANG];
  const cars = getFilteredSorted();
  const totalPages = Math.max(1, Math.ceil(cars.length / PER_PAGE));
  if (currentPage > totalPages) currentPage = totalPages;

  const start = (currentPage - 1) * PER_PAGE;
  const pageCars = cars.slice(start, start + PER_PAGE);

  const grid = document.getElementById('fleet-grid');
  const empty = document.getElementById('fleet-empty');
  const count = document.getElementById('fleet-count');

  count.textContent = t['fleet.showing'].replace('{count}', cars.length);

  if (pageCars.length === 0) {
    grid.innerHTML = '';
    empty.classList.remove('hidden');
  } else {
    empty.classList.add('hidden');
    grid.innerHTML = pageCars.map((c, i) => `
      <article class="car-card group relative border border-line bg-white reveal" style="transition-delay:${i*55}ms">
        <div class="relative aspect-[4/3] overflow-hidden">
          ${c.img ? `<img src="${c.img}" alt="${c.name}" class="car-media absolute inset-0 w-full h-full object-cover" />` : `<div class="car-media absolute inset-0 ph"></div>`}
          <div class="absolute left-4 bottom-4 z-10">
            <span class="mono-lbl inline-block px-2.5 py-1 bg-cream text-ink">${c.cat[LANG].toLocaleUpperCase(LANG === 'tr' ? 'tr-TR' : 'en-US')}</span>
          </div>
        </div>
        <div class="p-5 md:p-6">
          <div class="flex items-baseline justify-between gap-3">
            <h3 class="font-serif text-[20px] md:text-[24px] leading-tight text-cream tracking-tighter2 balance">${c.name}</h3>
            <span class="mono-lbl text-cream/40 stat-num shrink-0">${c.year}</span>
          </div>
          <div class="mt-3 md:mt-4 flex items-center gap-3 mono-lbl text-cream/50">
            <span class="stat-num">${c.seats} ${t['card.seats'].toLocaleUpperCase(LANG)}</span>
            <span class="text-line2">/</span>
            <span>${c.gear[LANG].toLocaleUpperCase('en-US')}</span>
            <span class="text-line2">/</span>
            <span class="stat-num">${c.hp} ${t['card.hp'].toLocaleUpperCase(LANG)}</span>
          </div>
          <div class="mt-4 md:mt-5 relative h-[1px] bg-line">
            <div class="car-rule absolute left-0 top-0 h-full bg-accent w-8 transition-all duration-700"></div>
          </div>
          <div class="mt-4 md:mt-5">
            <a href="${c.turoUrl || '#'}" target="_blank" rel="noopener" class="inline-flex items-center justify-center gap-2 w-full bg-gold text-white hover:bg-goldhi transition py-3 rounded-lg text-[11px] font-semibold tracking-[0.18em] uppercase">
              ${t['card.book']}
              <span class="car-cta-arrow inline-block transition-all duration-500">→</span>
            </a>
          </div>
        </div>
      </article>
    `).join('');
  }

  renderPagination(totalPages);
  observeReveals();
}

function renderPagination(totalPages) {
  const pag = document.getElementById('pagination');
  if (totalPages <= 1) { pag.innerHTML = ''; return; }

  let html = '';
  const btnClass = 'w-10 h-10 flex items-center justify-center rounded-lg text-[13px] font-medium transition';
  const activeClass = 'bg-gold text-white';
  const inactiveClass = 'bg-white border border-line text-cream/70 hover:border-gold hover:text-gold';

  if (currentPage > 1) {
    html += `<button class="${btnClass} ${inactiveClass}" data-page="${currentPage - 1}">←</button>`;
  }

  for (let i = 1; i <= totalPages; i++) {
    if (totalPages > 7 && i > 2 && i < totalPages - 1 && Math.abs(i - currentPage) > 1) {
      if (html.slice(-3) !== '...') html += `<span class="w-8 text-center text-cream/30">...</span>`;
      continue;
    }
    html += `<button class="${btnClass} ${i === currentPage ? activeClass : inactiveClass}" data-page="${i}">${i}</button>`;
  }

  if (currentPage < totalPages) {
    html += `<button class="${btnClass} ${inactiveClass}" data-page="${currentPage + 1}">→</button>`;
  }

  pag.innerHTML = html;
  pag.querySelectorAll('button[data-page]').forEach(btn => {
    btn.addEventListener('click', () => {
      currentPage = parseInt(btn.dataset.page);
      renderFleet();
      window.scrollTo({ top: document.getElementById('fleet-grid').offsetTop - 120, behavior: 'smooth' });
    });
  });
}

let io;
function observeReveals() {
  if (!io) {
    io = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); }
      });
    }, { threshold: 0.10, rootMargin: '0px 0px -40px 0px' });
  }
  document.querySelectorAll('.reveal:not(.in)').forEach(el => io.observe(el));
}

function applyI18nContent() {
  const t = I18N[LANG];
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const k = el.getAttribute('data-i18n');
    if (t[k] != null) el.innerHTML = t[k];
  });
  document.documentElement.lang = LANG;
  document.querySelector('.lang-track').setAttribute('data-lang', LANG);
  populateFilters();
  renderFleet();
}

function switchLang(next) {
  if (next === LANG) return;
  document.body.classList.add('lang-swap');
  setTimeout(() => {
    LANG = next;
    applyI18nContent();
    requestAnimationFrame(() => document.body.classList.remove('lang-swap'));
  }, 260);
}

document.querySelectorAll('.lang-pill').forEach(p => {
  p.addEventListener('click', () => switchLang(p.dataset.lang));
});

document.getElementById('filter-brand').addEventListener('change', () => { currentPage = 1; renderFleet(); });
document.getElementById('filter-cat').addEventListener('change', () => { currentPage = 1; renderFleet(); });
document.getElementById('filter-seats').addEventListener('change', () => { currentPage = 1; renderFleet(); });
document.getElementById('sort-by').addEventListener('change', () => { currentPage = 1; renderFleet(); });

const hamburger = document.querySelector('.hamburger');
const mobileMenu = document.querySelector('.mobile-menu');
hamburger.addEventListener('click', () => {
  hamburger.classList.toggle('active');
  mobileMenu.classList.toggle('open');
  document.body.style.overflow = mobileMenu.classList.contains('open') ? 'hidden' : '';
});
document.querySelectorAll('.mobile-link').forEach(link => {
  link.addEventListener('click', () => {
    hamburger.classList.remove('active');
    mobileMenu.classList.remove('open');
    document.body.style.overflow = '';
  });
});

async function loadLiveData() {
  try {
    const [carsRes, contentRes] = await Promise.all([
      fetch('/api/cars').then(r => r.ok ? r.json() : null),
      fetch('/api/content').then(r => r.ok ? r.json() : null)
    ]);
    if (carsRes) FLEET = carsRes;
    if (contentRes) {
      const s = contentRes.site;
      if (s?.whatsapp) {
        const el = document.getElementById('whatsapp-btn');
        if (el) el.href = 'https://wa.me/' + s.whatsapp.replace(/[^0-9]/g, '');
      }
    }
    populateFilters();
    renderFleet();
  } catch {}
}

populateFilters();
renderFleet();
observeReveals();
loadLiveData();
</script>
