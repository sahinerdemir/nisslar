{
  "name": "nisslar",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --port=8080",
    "build": "npx @11ty/eleventy",
    "css": "npx tailwindcss -i src/css/main.css -o _site/css/main.css --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
